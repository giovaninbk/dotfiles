return {
	"nvim-lualine/lualine.nvim",
	dependencies = { "nvim-tree/nvim-web-devicons" },
	opts = {
		options = {
			-- Removes section separators (e.g., > or } )
			section_separators = "",
			-- Removes component separators (e.g., | or , )
			component_separators = "",
		},
	},
}
